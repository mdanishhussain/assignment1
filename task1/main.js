var result = 0;
var a = 1;
var b = 2;
function doDivision(firstNo, secondNo) {
    window.alert("Div " + (firstNo / secondNo));
}
function doMultiplication(firstNo, secondNo) {
    window.alert("Mult " + (firstNo / secondNo));
}
function doAddition(firstNo, secondNo) {
    window.alert("Add " + (firstNo + secondNo));
}
function doSubstraction(firstNo, secondNo) {
    window.alert("Sub " + (firstNo - secondNo));
}
doAddition(a, b);
doSubstraction(a, b);
doMultiplication(a, b);
doDivision(a, b);
